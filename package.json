{
  "name": "dad-jokes-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^1.2.1",
    "express": "^4.17.1",
    "react": "^16.8.1",
    "react-dom": "^16.8.1",
    "react-scripts": "^5.0.1",
    "uuid": "^3.3.2"
  },
  "scripts": {
    "start": "start": "set PORT=3100 && node scripts/start.js",
    "build": "SET NODE_OPTIONS=--openssl-legacy-provider && react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "parcel": "^2.8.1"
  }
}
